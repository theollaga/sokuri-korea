# Sokuri Korea

Hugo project for Sokuri Korea site.

## Structure

- `layouts/` - Hugo templates
- `assets/css/` - CSS files
- `content/` - (to be added)
- `data/` - (to be added)
- `static/` - (to be added)

## Development

To run a local server:

```bash
hugo server
```

To build for production:

```bash
hugo --destination /tmp/sokuri-build
```